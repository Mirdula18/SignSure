# Frontend build variables (these ARE shipped to the browser - never put a secret here).
# Cloudflare's always-passes Turnstile test site key; replace with your real site key in production.
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
